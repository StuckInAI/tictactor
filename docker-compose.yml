version: '3.8'
services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    volumes:
      - ./server:/app
      - /app/node_modules
    command: npm start
  client:
    build: ./client
    ports:
      - "3000:80"
    depends_on:
      - server
